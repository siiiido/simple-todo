export interface Todo {
	id: string,
	todoText: string,
	checkbox: boolean
};

// styled components
export type Button = {
    background?: string;
    color?: string;
}

export type ButtonnContainer = {
  margtinTop: string;
}
 
export type Checked = {
	checked: boolean;
}